{
  "name": "reckoner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Comprehension guardrails for humans — a predict-then-reveal gate between an AI agent's proposal and your approval.",
  "license": "MIT",
  "scripts": {
    "demo": "tsx src/cli.ts",
    "gate": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
